//// Definition for binary tree:// function Tree(x) {//   this.value = x;//   this.left = null;//   this.right = null;// }function solution(t) {    let levels = [];    for ( let i = 0; i < 50001; i++ ){        levels.push(-1001);    }        function traverse(obj, level) {        if (!obj) return;        levels[level] < obj.value ? levels[level] = obj.value : '';                level++;        traverse(obj.left, level);        traverse(obj.right, level);    }        traverse(t, 0)            let results = [];        for ( let i = 0; i < levels.length; i++){        if ( levels[i] !== -1001 ){            results.push(levels[i])

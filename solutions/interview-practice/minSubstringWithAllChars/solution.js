function solution(s, t) {    s = s.split('');    t = t.split('');    let obj = {};    let subStrings = [];    for ( let i = 0; i < s.length; i++ ){                    for ( let k = 0; k < t.length; k++ ){            if ( t[k] === s[i] ){                     for( var x = 0; x < t.length; x++) {                    obj[t[x]] = false;                }                                let sequence = [];                for ( let j = i; j < s.length; j++ ){                    sequence.push(s[j]);                                        if ( obj.hasOwnProperty(s[j]) ){                        obj[s[j]] = true;                                                let isAllTrue = true;                        for ( let key in obj ){                            if ( obj[key] == false ){                                isAllTrue = false;                                break;                            }                         }                                                if ( isAllTrue ){                            subStrings.push(sequence);                            break;                        }
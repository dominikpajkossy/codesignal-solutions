function solution(l1, l2) {    let arr1 = [];    let arr2 = [];    while (l1 != null){        arr1.push(l1.value);        l1 = l1.next;    }    while (l2 != null){        arr2.push(l2.value);        l2 = l2.next;    }    arr1.reverse();    arr2.reverse();        function addTwo(arr1,arr2){        let retArr = [];        let carry = 0;                for ( var i = 0; i < arr1.length; i++ ){            if ( typeof arr2[i] != "undefined" ){                let num = arr1[i] + arr2[i] + carry;                if ( num >= 10000 ){                    carry = 1;                    retArr.push( num % 10000 );                }else{                    retArr.push( num );                    carry = 0;                }            }else{                let num = arr1[i] + carry;                if ( num >= 10000 ){                    carry = 1;
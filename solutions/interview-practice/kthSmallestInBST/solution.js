//// Binary trees are already defined with this interface:// function Tree(x) {//   this.value = x;//   this.left = null;//   this.right = null;// }function solution(t, k) {    let i = 0;    let result;    traverse(t);    function traverse(t){        if ( t.left ){            traverse(t.left);        }        i+=1;        if (i === k) result = t.value;        if ( !t.left && !t.right ){            return;        }        if ( t.right ){            traverse(t.right);        }    }    return result;}

function solution(grid) {    let numCheck = [0,0,0,0,0,0,0,0,0];        //Horizontal check    for ( var i = 0; i < grid.length; i++){        for ( var j = 0; j < grid[0].length; j++){            if ( numCheck[grid[i][j]-1] === 0 ){                numCheck[grid[i][j]-1] = 1;            }else{                return false;            }        }        numCheck = [0,0,0,0,0,0,0,0,0];    }        //Vertical check    for ( var i = 0; i < grid[0].length; i++){        for ( var j = 0; j < grid.length; j++){            if ( numCheck[grid[j][i]-1] === 0 ){                numCheck[grid[j][i]-1] = 1;            }else{                return false;            }        }        numCheck = [0,0,0,0,0,0,0,0,0];    }        //Grid check            let x = 0,        y = 0;
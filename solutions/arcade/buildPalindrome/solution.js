function solution(st) {    function checkPalindrome(inputString) {        let length = Math.floor(inputString.length / 2)        let firstHalf = [];        let secondHalf = [];            for ( var i = 0; i < length; i++){            firstHalf.push(inputString[i])        }        for ( var i = inputString.length-1; i >= length; i--){            secondHalf.push(inputString[i]);        }        for ( var i = 0; i < firstHalf.length; i++){            if ( firstHalf[i] != secondHalf[i] ){                return false;            }        }        return true;    }    if (checkPalindrome(st) ) return st;        for ( var i = 0; i < st.length; i++){        let revString = [];        for ( var j = i; j >= 0; j--){            revString.push(st[j]);        }        revString = revString.join("");        if (checkPalindrome(st + revString) ) return st + revString;    }}
def solution(s):    counted = []    num = []    for i in range(0,len(s)):        if s[i] not in counted:            num.append(s.count(s[i]))            counted.append(s[i])    digits = sum(num)    num = sorted(num)    print(int(math.ceil(pow(pow(10,digits),.5))))    for j in range(int(math.ceil(pow(pow(10,digits),.5))),0,-1):        sq = str(int(pow(j,2)))        counted = []        numSq = []        for i in range(0,len(sq)):            if sq[i] not in counted:                numSq.append(sq.count(sq[i]))                counted.append(sq[i])        numSq = sorted(numSq)        if num == numSq:            return int(sq)        return -1
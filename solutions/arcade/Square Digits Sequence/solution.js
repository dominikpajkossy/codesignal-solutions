function solution(a0) {    let i = 0,        arrCheck = [],        count = 1;    while ( i < 650 ){        arrCheck.push(0);        i++;    }    arrCheck[a0.toString()] = -1;        while ( 1 ){        let numStr = a0.toString();        let sum = 0;        for ( var j = 0; j < numStr.length; j++){            sum += Number(numStr[j]) * Number(numStr[j]);        }        if ( arrCheck[sum] === -1 ) return ++count        else arrCheck[sum] = -1        a0 = sum;        count++;    }}
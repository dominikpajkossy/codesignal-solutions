function solution(n) {        let matrix = [];    for ( var i = 0; i < n; i++){        matrix.push([]);        for ( var j = 0; j < n; j++){            matrix[i].push(0);        }    }    let nXn = n*n;    let x = 0,        y = 0,        dir = 0,        margin = 0;        for ( var i = 1; i <= nXn; i++){        if ( dir === 0 ){            if ( matrix[0].length - 1 - margin === y )                dir = 1;            else{                matrix[x][y] = i;                y++;            }        }        if ( dir === 1 ){            if ( matrix.length - 1 - margin === x )                dir = 2;            else{                matrix[x][y] = i;                x++;            }        }
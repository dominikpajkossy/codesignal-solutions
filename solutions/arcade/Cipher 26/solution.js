function solution(message) {    let retArr = [];    let sum = message[0].charCodeAt()-97;    retArr.push( message[0] );        for ( var i = 1; i < message.length; i++){        let enChar = (message[i].charCodeAt() - 97);        if ( sum < 26 ){                if ( enChar >= (sum % 26) ){                       var tmp = Math.abs(enChar - sum );                }else{                       var tmp = 26 - Math.abs(enChar - sum );                }        }else{               if ( enChar >= (sum % 26) ){                       var tmp =  enChar - (sum % 26);               }else{                       var tmp = 26 - Math.abs(enChar - (sum % 26));               }        }                    let char = String.fromCharCode(tmp + 97);        retArr.push( char );                retArr = retArr.join("");        sum += retArr[retArr.length-1].charCodeAt() - 97;        retArr = retArr.split("");            }    return retArr.join("");
function solution(matrix) {    let squares = [];    for ( var i = 0; i < matrix.length-1; i++){        for ( var j = 0; j < matrix[0].length-1; j++){            let tmp = [];            tmp.push(matrix[i][j])            tmp.push(matrix[i][j+1])            tmp.push(matrix[i+1][j])            tmp.push(matrix[i+1][j+1])                        let hasElem = false;            for ( var k = 0; k < squares.length; k++){                                let hasEqual = true;                for ( var l = 0; l < tmp.length; l++){                    if ( tmp[l] !== squares[k][l] ) hasEqual = false;                }                if ( hasEqual == true ) hasElem = true;            }            if ( !hasElem ) squares.push(tmp);        }    }    return squares.length;}
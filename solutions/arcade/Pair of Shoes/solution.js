function solution(shoes) {    let retObj = {        //23 : [0,1,0],    };    for ( var i = 0; i < shoes.length; i++ ){        if ( retObj[shoes[i][1]] ){            retObj[shoes[i][1]].push( shoes[i][0] )        }else{            retObj[shoes[i][1]] = [shoes[i][0]];        }    }        for ( x in retObj ){        let oneCount = 0;        let zeroCount = 0;        for ( var i = 0; i < retObj[x].length; i++ ){            if ( retObj[x][i] === 1 ){                oneCount++;            }else{                zeroCount++;            }        }        if ( zeroCount !== oneCount ) return false;    }        return true;}
function solution(sequence) {    //get the removable elements    let elementsToRemove = [];    let prevElement = sequence[0];        for (var i = 1; i < sequence.length; i++){        if ( prevElement >= sequence[i] ){            if (i === 1){                elementsToRemove.push(i-1);            }else{                if (sequence[i] > sequence[i-2]){                    elementsToRemove.push(i-1);                }else{                    elementsToRemove.push(i);                }                            }        }        prevElement = sequence[i];    }    console.log(elementsToRemove);                         let tmpSeq = sequence; for ( let i = 0; i < elementsToRemove.length; i++){   let ignore = elementsToRemove[i];   let isIncreasing = true;   let prev;   let j;   if ( ignore === 0){    prev = tmpSeq[1];    j = 2;
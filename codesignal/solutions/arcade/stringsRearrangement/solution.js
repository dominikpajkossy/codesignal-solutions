function solution(inputArray) {        function diff( a, b ){        let difference = 0;        for ( var i = 0; i < a.length; i++ ){            if ( a[i] != b[i] ) difference++;        }        return difference === 1;    }        function hamiltonian(currElem, Arr){        if (Arr.length === 0 ) return true;        for ( var i = 0; i < Arr.length; i++){            if ( diff(currElem, Arr[i] ) ){                let passArray = [];                for ( var j = 0; j < Arr.length; j++){                    if ( j != i ) passArray.push(Arr[j]);                }                console.log("passArray:" + passArray + " passElem:" + Arr[i] );                if ( hamiltonian(Arr[i], passArray) ){                    return true;                }            }        }        return false;    }        for ( var i = 0; i < inputArray.length; i++){        let passArr = [];        for ( var j = 0; j < inputArray.length; j++){            if ( j != i ) passArr.push(inputArray[j]);
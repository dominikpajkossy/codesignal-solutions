function solution(a) {    let toSortArr = [];    let treeIndexArr = [];    for ( var i = 0; i < a.length; i++){        if ( a[i] != -1 ){            toSortArr.push(a[i]);        }else{            treeIndexArr.push(i);        }    }    toSortArr.sort(function(a,b){        return a-b;    })    console.log(treeIndexArr);    let returnArr = [];    let indexCounter = 0;    let elementCounter = 0;    for ( var i = 0; i < a.length; i++){        if ( i === treeIndexArr[indexCounter] ){            returnArr.push(-1);            indexCounter++;        }else{            returnArr.push(toSortArr[elementCounter]);            elementCounter++;        }    }        return returnArr;}
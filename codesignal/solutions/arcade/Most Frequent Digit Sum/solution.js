function solution(n) {    let num = n;    let arrOfNums = [];        if ( num.toString().length != 1 ) {        while ( num != 0 ){            let arr = num.toString().split("");                    let sum = 0;            for ( k in arr ){                sum += Number(arr[k]);            }                    arrOfNums.push(sum);            num -= sum;        }        arrOfNums.push(0);    }else{        arrOfNums.push(num);        arrOfNums.push(0);    }        let hash = {length : 0};    for ( var i = 0; i < arrOfNums.length; i++ ){        if ( hash.hasOwnProperty(arrOfNums[i]) ){            hash[arrOfNums[i]] += 1;        }else{            hash[arrOfNums[i]] = 1;            hash.length += 1;        }    }    
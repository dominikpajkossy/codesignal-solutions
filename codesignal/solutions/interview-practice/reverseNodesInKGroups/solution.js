// Definition for singly-linked list:// function ListNode(x) {//   this.value = x;//   this.next = null;// }//function solution(l, k) {    let count = k;    let listArr = [];    let retArr = [];    let x = 0;        while(l != null){        if ( count > 0 ){            if (listArr.length < k) listArr.push(l);            count--;        }else{            retArr.push([]);            for ( var i = 0; i < listArr.length; i++ ){                retArr[x].push(listArr[i]);            }            x++;                        listArr = [];            if (listArr.length < k) listArr.push(l);            count = k-1;        }        l = l.next;    }        if ( listArr.length === k ){        retArr.push([]);

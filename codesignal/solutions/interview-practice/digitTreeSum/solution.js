//// Definition for binary tree:// function Tree(x) {//   this.value = x;//   this.left = null;//   this.right = null;// }function solution(t) {    let sums = [];    function traverse(t, sum){        if ( !t ) return;                sum = sum += t.value;                traverse(t.left, sum);        traverse(t.right, sum);                if ( !t.left && !t.right ){            sums.push(sum);        }    }        traverse(t, '');        let total = 0;    sums.map(sum=>{        total += Number(sum);    })        return total

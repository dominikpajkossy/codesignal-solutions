function solution(strings, patterns) {    let hash1 = {};    let hash2 = {};        for ( var i = 0; i < strings.length; i++){        if ( hash1.hasOwnProperty(strings[i]) ){            hash1[strings[i]].push(i);        }else{            hash1[strings[i]] = [i];        }    }        for ( var i = 0; i < patterns.length; i++){        if ( hash2.hasOwnProperty(patterns[i]) ){            hash2[patterns[i]].push(i);        }else{            hash2[patterns[i]] = [i];        }    }        let ex1 = [];    let ex2 = [];        for ( var key in hash1 ){        ex1.push(hash1[key]);    }    for ( var key in hash2 ){        ex2.push(hash2[key]);    }    if ( ex1.length == ex2.length ){        for ( var i in ex1 ){            if ( ex1[i].length == ex2[i].length ){
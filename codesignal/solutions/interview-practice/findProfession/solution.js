function solution(level, pos) {    if ( level == 1 ) return "Engineer";    if ( level == 2 ){        if (pos == 1){            return "Engineer";        }else{            return "Doctor";        }    }    if ( pos % 2 == 1 ){        return solution(level - 1, (pos+1)/2);    }else{        if (solution(level - 1, pos/2) == "Doctor"){            return "Engineer"        }else{            return "Doctor"        }    }}    /* Build tree and traverse - Too slow    let tree = {prof : "Engineer", left : null, right : null};    let toVisit = [tree];    for ( var d = 1; d <= level; d++){        let tmp = [];        for(var i = 0; i < toVisit.length; i++){            if ( d === level && i+1 === pos ) return toVisit[i].prof;            if (toVisit[i].prof == "Engineer"){                toVisit[i].left = {prof : "Engineer", left : null, right : null};
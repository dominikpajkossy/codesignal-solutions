//// Definition for binary tree:// function Tree(x) {//   this.value = x;//   this.left = null;//   this.right = null;// }function solution(t) {    if( t == null ) return true;    if( t.left == null || t.right == null ) return false;    let lArr = [];    let rArr = [];    function traverseLeft(t,parentDir){                if ( t.value != null) lArr.push(t.value+parentDir);                if (t.left != null){            traverseLeft(t.left,"l");        }        if (t.right != null){            traverseLeft(t.right,"r");        }    }        function traverseRight(t,parentDir){                if ( t.value != null) rArr.push(t.value+parentDir);                if (t.right != null){            traverseRight(t.right,"l");
